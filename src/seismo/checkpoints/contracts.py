"""Pydantic contracts for the two LLM checkpoints (docs 05, 08).

Populated in Stage 4 (comprehension) and Stage 7 (impact). Kept here so the import
site is stable and the CI invariant-3 grep has a home to enforce from Stage 0 onward.
"""

from __future__ import annotations
