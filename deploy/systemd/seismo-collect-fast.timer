# Daily 05:30 UTC (doc 03 §3). Persistent=true catches up after downtime; collectors are
# idempotent so a catch-up run inserts zero duplicates.
[Unit]
Description=Seismograph fast collector (daily)

[Timer]
OnCalendar=*-*-* 05:30 UTC
Persistent=true

[Install]
WantedBy=timers.target
